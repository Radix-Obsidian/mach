# Mach Unified Server Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# Supabase Configuration
# =============================================================================
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-role-key
# Webhook signature secret (optional - for production security)
# Generate with: openssl rand -hex 32
SUPABASE_WEBHOOK_SECRET=

# =============================================================================
# AI Provider Configuration
# =============================================================================
ANTHROPIC_API_KEY=sk-ant-api03-...

# OpenAI API key for Mach-Trace vector embeddings (text-embedding-3-small)
# Required for Mach-Trace contextual validation. Without this, trace is skipped.
OPENAI_API_KEY=sk-...

# Optional: Override default model
# MACH_PROVIDER=anthropic
# MACH_MODEL=claude-3-5-sonnet-latest

# =============================================================================
# Server Configuration
# =============================================================================
PORT=8080
NODE_ENV=development

# Enable polling mode (useful for development without webhooks)
# Set to 1 to enable polling instead of webhook-triggered processing
MACH_POLLING=1

# Polling interval in milliseconds (default: 5000)
# MACH_POLL_INTERVAL_MS=5000
